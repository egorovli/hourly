{
	"name": "@hourly/api",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "run-p dev:serve",
		"dev:serve": "bun run --hot --no-clear-screen ./src/index.ts",
		"types:check": "tsc",
		"test": "bun test --randomize",
		"test:watch": "bun test --watch --randomize",
		"test:coverage": "bun test --coverage --coverage-reporter=lcov --randomize"
	},
	"devDependencies": {
		"@total-typescript/ts-reset": "^0.6.1",
		"@types/bun": "^1.3.1",
		"npm-run-all": "^4.1.5"
	},
	"peerDependencies": {
		"typescript": "^5.6.2"
	},
	"dependencies": {
		"commander": "^14.0.2",
		"elysia": "^1.4.13",
		"inversify": "^7.10.4",
		"zod": "^4.1.12"
	}
}

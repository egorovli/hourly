import { DomainError } from '../base/domain-error.ts'

export class BusinessRuleError extends DomainError {
	// constructor(message: string, details?: unknown) {
	// 	super(message, details) // Unprocessable Entity
	// }
}
